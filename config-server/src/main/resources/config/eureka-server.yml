server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

info:
  app:
    name: eureka-server
    description: Servidor de descubrimiento de servicios
    version: 1.0.0